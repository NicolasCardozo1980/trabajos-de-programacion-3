const nombres: string[] = ["Juan", "Ana", "Pedro", "Maria", "Luisa", "Carlos"];

for (let i = 0; i < nombres.length; i++) {
  const nombre = nombres[i];
  console.log(`El nombre "${nombre}" tiene ${nombre.length} caracteres`);
}
